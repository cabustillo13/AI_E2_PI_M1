# AI_E2_PI_M1

## Ejecutar la API

Instala las dependencias:

```bash
pip install -r requirements.txt
```

Desde la raiz del proyecto, inicia Uvicorn:

```bash
uvicorn src.main:app --reload
```

La API queda disponible en `http://127.0.0.1:8000`.